from app.models.audio_processor import AudioProcessor
from app.models.beat_detector import BeatDetector
from app.models.character_generator import CharacterGenerator
from app.models.lip_sync_engine import LipSyncEngine
from app.models.instrument_simulator import InstrumentSimulator
from app.models.scene_composer import SceneComposer
from app.models.video_renderer import VideoRenderer
# Remove imports for face_generator, emotion_analyzer, style_transfer, video_enhancer, human_movement, facial_expressions, voice_cloner, music_style_replicator
# or keep them but make sure they don't require heavy libs (they don't, they use fallbacks)
